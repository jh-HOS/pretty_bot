function response(room, msg, sender, isGroupChat, replier, imageDB) {
  if (room == "🍓쁘 띠 공 지 방🍓") {
    if (msg.indexOf("/") == 0 && msg.indexOf("파티") != -1) {
      if (msg.sender) {
      }
    }
  }
}
